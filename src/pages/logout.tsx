

export default function Logout() {
  const handleLogout = async () => {
    
  };

  return (
    <div>
      <h1>Logout</h1>
      <button onClick={handleLogout}>Sign out</button>
    </div>
  );
}